[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not slow and not requires_ml"
markers =
    slow: large fixtures or long-running integration cases
    requires_ml: needs numpy, pandas, and tqdm (compare pipeline)
